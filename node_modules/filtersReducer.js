import { SET_FILTER } from "../actions/booksActions";
const initial = { author: "", genre: "", status: "all", search: "" };
export default (s = initial, a) => a.type === SET_FILTER ? { ...s, ...a.payload } : s;
